# Architecture

Architecture documentation for AgentFace. See `docs/decisions/` for ADRs.

Planned documents (written as the corresponding phases land):

- Package graph and boundaries
- Runtime model: definitions, mounted instances, live registrations
- Action lifecycle and confirmation binding
- Policy evaluation
- Trace and event model
